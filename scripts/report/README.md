This folder contains report tooling.

- `make_report.py`: reads an output folder (must contain `summary.json`) and writes `report.tex`. If `latexmk` or `pdflatex` is available on PATH, it also builds `report.pdf`.
